# tell django the name of the defaullt config class for the app
# this is required for let signals work in django
# this class is present in checkout/apps.py:
default_app_config = 'checkout.apps.CheckoutConfig'
